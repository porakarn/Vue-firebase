 <v-flex xs7 sm8 md9>
                <v-card-title primary-title>
                  <div>
                    <div>รหัส ----  {{ course.id }}</div>
                    <div>วิชา ----    {{ course.subject }}</div>
                    <div> {{ course.purpose }}</div>
                    <div>รายละเอียดติวเตอร์  ----  {{ course.tutor }}</div>
                    <div>เพศติวเตอร์ ---- {{ course.gendertutor }}</div>
                    <div>เพศนักเรียน ----  {{course.gender }}</div>
                    <div>ชั้น ----  {{ course.gradeDetail }}</div>
                    <div>จำนวนนักเรียน ---- {{ course.numberstudent }}</div>
                    <div>โรงเรียน ---- {{ course.school }}</div>
                    <div>สถานที่สอน ---- {{ course.location }}</div>
                    <div>วันที่เรียน ---- {{ course.e6 }}</div>
                    <div>เวลาที่เรียน ---- {{ course.time }}</div>
                    <div>ค่าสอน{{ course.revenue }}</div> 
                    <div>ค่าแนะนำ{{ course.fee }}</div>
                  </div>
                </v-card-title>
                <!-- <v-card-actions>
                  <v-btn flat :to="'/meetups/' + meetup.id">
                    <v-icon left light>arrow_forward</v-icon>
                    View Meetup
                  </v-btn>
                </v-card-actions> -->
              </v-flex>